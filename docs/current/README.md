# Current docs

Secondary docs that are still true and maintained.

Primary sources of truth stay at the repo root:

- `PROJECT.md` — product intent
- `CONTEXT.md` — architecture
- `LANGUAGE.md` — domain language
- `CODE-STYLE.md` — style and structure
- `AGENTS.md` — agent routing

When a file here goes stale, move it to `docs/archive/` (or delete it) in the
same change that replaces it. Do not leave outdated guidance in `current/`.
